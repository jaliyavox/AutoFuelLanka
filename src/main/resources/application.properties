spring.application.name=systemmanager
server.port=8080

# --- MySQL ---
spring.datasource.url=jdbc:mysql://localhost:3306/manager?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
spring.datasource.username=root
spring.datasource.password=root123
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# --- JPA ---
# First run: use create to generate fresh tables
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.open-in-view=false

# --- Run data.sql if present ---
spring.jpa.defer-datasource-initialization=true
spring.sql.init.mode=always
